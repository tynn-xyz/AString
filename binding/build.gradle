plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.kapt'
    id 'maven-publish'
}

android {
    namespace "${group}.binding"

    buildFeatures {
        androidResources true
        dataBinding true
    }
}

dependencies {
    api project(':astring')
}
