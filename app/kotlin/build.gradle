plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'xyz.tynn.astring.app.kotlin'

    defaultConfig {
        versionCode 1
        versionName '0.0'
    }
}

dependencies {
    implementation project(':app:shared')
}
