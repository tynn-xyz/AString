plugins {
    id 'com.android.application'
}

android {
    namespace 'xyz.tynn.astring.app.java'

    defaultConfig {
        versionCode 1
        versionName '0.0'
    }
}

dependencies {
    implementation project(':app:shared'), {
        exclude group: 'org.jetbrains.kotlin'
    }
}
