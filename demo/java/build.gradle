plugins {
    id 'com.android.application'
}

android {
    namespace 'xyz.tynn.astring.demo.java'

    defaultConfig {
        versionCode 1
        versionName '0.0'
    }
}

dependencies {
    implementation project(':demo:shared'), {
        exclude group: 'org.jetbrains.kotlin'
    }
}
