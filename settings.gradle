pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

include ':bom'

include ':astring'

include ':widget:core'
include ':widget:appcompat'
include ':widget:material'

rootProject.name = 'AStrings'

enableFeaturePreview 'TYPESAFE_PROJECT_ACCESSORS'
