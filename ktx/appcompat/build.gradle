plugins {
    id 'com.android.library'
    id 'maven-publish'
}

android {
    namespace 'xyz.tynn.astring.appcompat'
}

dependencies {
    api project(':base:astring')
    api project(':ktx:core')
    api 'androidx.appcompat:appcompat:1.4.2'

    testImplementation project(':base:testing')
}
