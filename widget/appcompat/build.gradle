plugins {
    id 'com.android.library'
    id 'maven-publish'
}

android {
    namespace "${group}.appcompat"
}

dependencies {
    api project(':widget:core')
    api libs.androidx.appcompat

    compileOnly libs.androidx.databinding.common
}
